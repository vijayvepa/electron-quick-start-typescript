export enum AppEvents {
    ready = 'ready',
    windowAllClosed = 'window-all-closed',
    activate = 'activate'
}

export enum WindowEvents{
    closed = 'closed',
}

export enum Platforms {
    darwin = 'darwin'
}